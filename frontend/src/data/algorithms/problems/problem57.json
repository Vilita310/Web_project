{
  "meta": {
    "id": 57,
    "title": "插入区间",
    "difficulty": "中等",
    "leetcodeId": 57,
    "category": "array",
    "patterns": [
      "interval_merge"
    ],
    "tags": [
      "数组",
      "区间"
    ],
    "estimatedTime": "25分钟",
    "passingRate": "50.0%",
    "version": "1.0",
    "lastUpdated": "2025-10-21"
  },
  "description": "给你一个 无重叠的 ，按照区间起始端点排序的区间列表。在列表中插入一个新的区间，你需要确保列表中的区间仍然有序且不重叠（如果有必要的话，可以合并区间）。",
  "examples": [
    {
      "input": "intervals = [[1,3],[6,9]], newInterval = [2,5]",
      "output": "[[1,5],[6,9]]",
      "explanation": "新区间 [2,5] 与 [1,3] 重叠，合并为 [1,5]"
    },
    {
      "input": "intervals = [[1,2],[3,5],[6,7],[8,10],[12,16]], newInterval = [4,8]",
      "output": "[[1,2],[3,10],[12,16]]",
      "explanation": "新区间 [4,8] 与 [3,5]、[6,7]、[8,10] 重叠，合并为 [3,10]"
    }
  ],
  "constraints": [
    "0 <= intervals.length <= 10^4",
    "intervals[i].length == 2",
    "0 <= start_i <= end_i <= 10^5",
    "intervals 根据 start_i 按升序排列",
    "newInterval.length == 2",
    "0 <= start <= end <= 10^5"
  ],
  "hints": [
    "找到新区间应该插入的位置",
    "合并所有重叠的区间",
    "处理三种情况：在新区间之前、重叠、在新区间之后"
  ],
  "followUp": "你能想出更优化的解法吗？考虑时间和空间复杂度。",
  "solutions": [
    {
      "id": "linear_scan",
      "name": "线性扫描",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(n)",
      "description": "线性扫描找到插入位置并合并重叠区间",
      "code": {
        "javascript": "/**\n * 插入区间 - 线性扫描找到插入位置并合并重叠区间\n */\n\nfunction insert(intervals, newInterval) {\n    const result = [];\n    let i = 0;\n\n    // 添加所有在新区间之前的区间\n    while (i < intervals.length && intervals[i][1] < newInterval[0]) {\n        result.push(intervals[i]);\n        i++;\n    }\n\n    // 合并所有重叠的区间\n    while (i < intervals.length && intervals[i][0] <= newInterval[1]) {\n        newInterval[0] = Math.min(newInterval[0], intervals[i][0]);\n        newInterval[1] = Math.max(newInterval[1], intervals[i][1]);\n        i++;\n    }\n    result.push(newInterval);\n\n    // 添加所有在新区间之后的区间\n    while (i < intervals.length) {\n        result.push(intervals[i]);\n        i++;\n    }\n\n    return result;\n}\n\n\n/**\n * 测试insert函数\n */\nfunction testInsert() {\n    console.log(\"=== insert测试 ===\");\n\n    // 测试用例\n    console.log(\"输入: intervals = [[1,3],[6,9]], newInterval = [2,5]\");\n    console.log(\"期望输出: [[1,5],[6,9]]\");\n    console.log(\"说明: 新区间 [2,5] 与 [1,3] 重叠，合并为 [1,5]\");\n\n    console.log(\"\\n=== 测试完成 ===\");\n}\n\n// 如果需要运行测试，取消注释下面的行\n// testInsert();",
        "python": "\"\"\"\n插入区间 - 线性扫描找到插入位置并合并重叠区间\n\"\"\"\n\ndef insert(intervals, newInterval):\n    result = []\n    i = 0\n\n    # 添加所有在新区间之前的区间\n    while i < len(intervals) and intervals[i][1] < newInterval[0]:\n        result.append(intervals[i])\n        i += 1\n\n    # 合并所有重叠的区间\n    while i < len(intervals) and intervals[i][0] <= newInterval[1]:\n        newInterval[0] = min(newInterval[0], intervals[i][0])\n        newInterval[1] = max(newInterval[1], intervals[i][1])\n        i += 1\n    result.append(newInterval)\n\n    # 添加所有在新区间之后的区间\n    while i < len(intervals):\n        result.append(intervals[i])\n        i += 1\n\n    return result\n\n\ndef test_insert():\n    \"\"\"测试insert函数\"\"\"\n    print(\"=== insert测试 ===\")\n\n    # 测试用例\n    print(\"输入: intervals = [[1,3],[6,9]], newInterval = [2,5]\")\n    print(\"期望输出: [[1,5],[6,9]]\")\n    print(\"说明: 新区间 [2,5] 与 [1,3] 重叠，合并为 [1,5]\")\n\n    print(\"\\n=== 测试完成 ===\")\n\n# 如果需要运行测试，取消注释下面的行\n# test_insert()"
      }
    }
  ],
  "aiExplanation": {
    "concept": {
      "title": "🧠 插入区间解题策略",
      "content": "这是一道经典的算法题目，重点学习解题思路和代码实现。"
    },
    "keyInsights": [
      "💰 理解题目的核心要求",
      "📊 选择合适的算法策略",
      "🔄 优化时间和空间复杂度"
    ],
    "commonMistakes": [
      "❌ 没有考虑边界条件",
      "❌ 算法实现有误",
      "❌ 忽略了题目的特殊要求"
    ]
  },
  "visualization": {
    "type": "step_by_step",
    "defaultData": {
      "input": "intervals = [[1,3],[6,9]], newInterval = [2,5]",
      "output": "[[1,5],[6,9]]",
      "explanation": "新区间 [2,5] 与 [1,3] 重叠，合并为 [1,5]"
    },
    "steps": [
      {
        "step": 1,
        "description": "分析输入数据",
        "highlight": [],
        "action": "开始算法执行"
      }
    ]
  },
  "relatedProblems": [],
  "practiceExercises": [
    {
      "title": "插入区间变种练习",
      "description": "基于插入区间的变种问题",
      "difficulty": "中等",
      "hints": [
        "考虑边界情况",
        "优化算法复杂度"
      ]
    }
  ]
}