{
  "meta": {
    "id": 572,
    "title": "另一棵树的子树",
    "difficulty": "简单",
    "leetcodeId": 572,
    "category": "tree",
    "patterns": [
      "tree_subtree"
    ],
    "tags": [
      "树",
      "深度优先搜索",
      "字符串匹配",
      "二叉树",
      "哈希函数"
    ],
    "estimatedTime": "15分钟",
    "passingRate": "50.0%",
    "version": "1.0",
    "lastUpdated": "2025-10-21"
  },
  "description": "给你两棵二叉树 root 和 subRoot 。检验 root 中是否包含和 subRoot 具有相同结构和节点值的子树。如果存在，返回 true ；否则，返回 false 。二叉树 tree 的一棵子树包括 tree 的某个节点和这个节点的所有后代节点。tree 也可以看做它自身的一棵子树。",
  "examples": [
    {
      "input": "root = [3,4,5,1,2], subRoot = [4,1,2]",
      "output": "true",
      "explanation": "root中包含subRoot作为子树"
    },
    {
      "input": "root = [3,4,5,1,2,null,null,null,null,0], subRoot = [4,1,2]",
      "output": "false",
      "explanation": "root中不包含subRoot作为子树"
    }
  ],
  "constraints": [
    "root 树上的节点数量范围是 [1, 2000]",
    "subRoot 树上的节点数量范围是 [1, 1000]",
    "-10⁴ <= root.val <= 10⁴",
    "-10⁴ <= subRoot.val <= 10⁴"
  ],
  "hints": [
    "递归遍历主树的每个节点",
    "对每个节点检查是否与子树相同",
    "使用辅助函数判断两棵树是否相同"
  ],
  "followUp": "你能想出更优化的解法吗？考虑时间和空间复杂度。",
  "solutions": [
    {
      "id": "recursive",
      "name": "递归遍历",
      "timeComplexity": "O(m × n)",
      "spaceComplexity": "O(max(m, n))",
      "description": "递归遍历主树，检查每个子树",
      "code": {
        "javascript": "/**\n * 另一棵树的子树 - 递归遍历主树，检查每个子树\n */\n\nfunction isSubtree(root, subRoot) {\n    if (!root) return false;\n\n    // 检查当前节点为根的子树是否与subRoot相同\n    if (isSameTree(root, subRoot)) return true;\n\n    // 递归检查左右子树\n    return isSubtree(root.left, subRoot) || isSubtree(root.right, subRoot);\n}\n\nfunction isSameTree(p, q) {\n    if (!p && !q) return true;\n    if (!p || !q) return false;\n    if (p.val !== q.val) return false;\n\n    return isSameTree(p.left, q.left) && isSameTree(p.right, q.right);\n}\n\n\n/**\n * 测试isSubtree函数\n */\nfunction testIsSubtree() {\n    console.log(\"=== isSubtree测试 ===\");\n\n    // 测试用例\n    console.log(\"输入: root = [3,4,5,1,2], subRoot = [4,1,2]\");\n    console.log(\"期望输出: true\");\n    console.log(\"说明: root中包含subRoot作为子树\");\n\n    console.log(\"\\n=== 测试完成 ===\");\n}\n\n// 如果需要运行测试，取消注释下面的行\n// testIsSubtree();",
        "python": "\"\"\"\n另一棵树的子树 - 递归遍历主树，检查每个子树\n\"\"\"\n\ndef isSubtree(root, subRoot):\n    if not root:\n        return False\n\n    # 检查当前节点为根的子树是否与subRoot相同\n    if is_same_tree(root, subRoot):\n        return True\n\n    # 递归检查左右子树\n    return isSubtree(root.left, subRoot) or isSubtree(root.right, subRoot)\n\ndef is_same_tree(p, q):\n    if not p and not q:\n        return True\n    if not p or not q:\n        return False\n    if p.val != q.val:\n        return False\n\n    return is_same_tree(p.left, q.left) and is_same_tree(p.right, q.right)\n\n\ndef test_isSubtree():\n    \"\"\"测试isSubtree函数\"\"\"\n    print(\"=== isSubtree测试 ===\")\n\n    # 测试用例\n    print(\"输入: root = [3,4,5,1,2], subRoot = [4,1,2]\")\n    print(\"期望输出: true\")\n    print(\"说明: root中包含subRoot作为子树\")\n\n    print(\"\\n=== 测试完成 ===\")\n\n# 如果需要运行测试，取消注释下面的行\n# test_isSubtree()"
      }
    }
  ],
  "aiExplanation": {
    "concept": {
      "title": "🧠 另一棵树的子树解题策略",
      "content": "这是一道经典的算法题目，重点学习解题思路和代码实现。"
    },
    "keyInsights": [
      "💰 理解题目的核心要求",
      "📊 选择合适的算法策略",
      "🔄 优化时间和空间复杂度"
    ],
    "commonMistakes": [
      "❌ 没有考虑边界条件",
      "❌ 算法实现有误",
      "❌ 忽略了题目的特殊要求"
    ]
  },
  "visualization": {
    "type": "step_by_step",
    "defaultData": {
      "input": "root = [3,4,5,1,2], subRoot = [4,1,2]",
      "output": "true",
      "explanation": "root中包含subRoot作为子树"
    },
    "steps": [
      {
        "step": 1,
        "description": "分析输入数据",
        "highlight": [],
        "action": "开始算法执行"
      }
    ]
  },
  "relatedProblems": [],
  "practiceExercises": [
    {
      "title": "另一棵树的子树变种练习",
      "description": "基于另一棵树的子树的变种问题",
      "difficulty": "简单",
      "hints": [
        "考虑边界情况",
        "优化算法复杂度"
      ]
    }
  ]
}