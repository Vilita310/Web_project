{
  "meta": {
    "id": 124,
    "title": "二叉树中的最大路径和",
    "difficulty": "困难",
    "leetcodeId": 124,
    "category": "tree",
    "patterns": [
      "tree_path_sum"
    ],
    "tags": [
      "树",
      "深度优先搜索",
      "动态规划",
      "二叉树"
    ],
    "estimatedTime": "45分钟",
    "passingRate": "50.0%",
    "version": "1.0",
    "lastUpdated": "2025-10-21"
  },
  "description": "二叉树中的 路径 被定义为一条节点序列，序列中每对相邻节点之间都存在一条边。同一个节点在一条路径序列中 至多出现一次 。该路径 至少包含一个 节点，且不一定经过根节点。路径和 是路径中各节点值的总和。给你一个二叉树的根节点 root ，返回其 最大路径和 。",
  "examples": [
    {
      "input": "root = [1,2,3]",
      "output": "6",
      "explanation": "最优路径是 2 -> 1 -> 3 ，路径和为 2 + 1 + 3 = 6"
    },
    {
      "input": "root = [-10,9,20,null,null,15,7]",
      "output": "42",
      "explanation": "最优路径是 15 -> 20 -> 7 ，路径和为 15 + 20 + 7 = 42"
    }
  ],
  "constraints": [
    "树中节点数目范围是 [1, 3 * 10^4]",
    "-1000 <= Node.val <= 1000"
  ],
  "hints": [
    "对于每个节点，计算经过该节点的最大路径和",
    "递归计算左右子树的最大贡献值",
    "更新全局最大路径和"
  ],
  "followUp": "你能想出更优化的解法吗？考虑时间和空间复杂度。",
  "solutions": [
    {
      "id": "recursive_dfs",
      "name": "递归DFS",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(h)",
      "description": "递归计算每个节点的最大贡献值",
      "code": {
        "javascript": "/**\n * 二叉树中的最大路径和 - 递归计算每个节点的最大贡献值\n */\n\nfunction maxPathSum(root) {\n    let maxSum = -Infinity;\n\n    function maxGain(node) {\n        if (!node) return 0;\n\n        // 递归计算左右子树的最大贡献值\n        // 如果贡献值为负，则不选择该子树\n        const leftGain = Math.max(maxGain(node.left), 0);\n        const rightGain = Math.max(maxGain(node.right), 0);\n\n        // 当前节点的最大路径和：节点值 + 左子树贡献 + 右子树贡献\n        const currentMax = node.val + leftGain + rightGain;\n\n        // 更新全局最大路径和\n        maxSum = Math.max(maxSum, currentMax);\n\n        // 返回该节点的最大贡献值：节点值 + max(左子树贡献, 右子树贡献)\n        return node.val + Math.max(leftGain, rightGain);\n    }\n\n    maxGain(root);\n    return maxSum;\n}\n\n\n/**\n * 测试maxPathSum函数\n */\nfunction testMaxPathSum() {\n    console.log(\"=== maxPathSum测试 ===\");\n\n    // 测试用例\n    console.log(\"输入: root = [1,2,3]\");\n    console.log(\"期望输出: 6\");\n    console.log(\"说明: 最优路径是 2 -> 1 -> 3 ，路径和为 2 + 1 + 3 = 6\");\n\n    console.log(\"\\n=== 测试完成 ===\");\n}\n\n// 如果需要运行测试，取消注释下面的行\n// testMaxPathSum();",
        "python": "\"\"\"\n二叉树中的最大路径和 - 递归计算每个节点的最大贡献值\n\"\"\"\n\ndef maxPathSum(root):\n    max_sum = float('-inf')\n\n    def max_gain(node):\n        nonlocal max_sum\n        if not node:\n            return 0\n\n        # 递归计算左右子树的最大贡献值\n        # 如果贡献值为负，则不选择该子树\n        left_gain = max(max_gain(node.left), 0)\n        right_gain = max(max_gain(node.right), 0)\n\n        # 当前节点的最大路径和\n        current_max = node.val + left_gain + right_gain\n\n        # 更新全局最大路径和\n        max_sum = max(max_sum, current_max)\n\n        # 返回该节点的最大贡献值\n        return node.val + max(left_gain, right_gain)\n\n    max_gain(root)\n    return max_sum\n\n\ndef test_maxPathSum():\n    \"\"\"测试maxPathSum函数\"\"\"\n    print(\"=== maxPathSum测试 ===\")\n\n    # 测试用例\n    print(\"输入: root = [1,2,3]\")\n    print(\"期望输出: 6\")\n    print(\"说明: 最优路径是 2 -> 1 -> 3 ，路径和为 2 + 1 + 3 = 6\")\n\n    print(\"\\n=== 测试完成 ===\")\n\n# 如果需要运行测试，取消注释下面的行\n# test_maxPathSum()"
      }
    }
  ],
  "aiExplanation": {
    "concept": {
      "title": "🧠 二叉树中的最大路径和解题策略",
      "content": "这是一道经典的算法题目，重点学习解题思路和代码实现。"
    },
    "keyInsights": [
      "💰 理解题目的核心要求",
      "📊 选择合适的算法策略",
      "🔄 优化时间和空间复杂度"
    ],
    "commonMistakes": [
      "❌ 没有考虑边界条件",
      "❌ 算法实现有误",
      "❌ 忽略了题目的特殊要求"
    ]
  },
  "visualization": {
    "type": "step_by_step",
    "defaultData": {
      "input": "root = [1,2,3]",
      "output": "6",
      "explanation": "最优路径是 2 -> 1 -> 3 ，路径和为 2 + 1 + 3 = 6"
    },
    "steps": [
      {
        "step": 1,
        "description": "分析输入数据",
        "highlight": [],
        "action": "开始算法执行"
      }
    ]
  },
  "relatedProblems": [],
  "practiceExercises": [
    {
      "title": "二叉树中的最大路径和变种练习",
      "description": "基于二叉树中的最大路径和的变种问题",
      "difficulty": "困难",
      "hints": [
        "考虑边界情况",
        "优化算法复杂度"
      ]
    }
  ]
}