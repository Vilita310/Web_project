{
  "meta": {
    "id": 23,
    "title": "合并K个升序链表",
    "difficulty": "困难",
    "leetcodeId": 23,
    "category": "linked_list",
    "patterns": [
      "divide_conquer_merge"
    ],
    "tags": [
      "链表",
      "分治",
      "堆"
    ],
    "estimatedTime": "45分钟",
    "passingRate": "50.0%",
    "version": "1.0",
    "lastUpdated": "2025-10-21"
  },
  "description": "给你一个链表数组，每个链表都已经按升序排列。请你将所有链表合并到一个升序链表中，返回合并后的链表。",
  "examples": [
    {
      "input": "lists = [[1,4,5],[1,3,4],[2,6]]",
      "output": "[1,1,2,3,4,4,5,6]",
      "explanation": "合并后的链表为: 1->1->2->3->4->4->5->6"
    },
    {
      "input": "lists = []",
      "output": "[]",
      "explanation": "空数组返回空链表"
    },
    {
      "input": "lists = [[]]",
      "output": "[]",
      "explanation": "只有一个空链表"
    }
  ],
  "constraints": [
    "k == lists.length",
    "0 <= k <= 10^4",
    "0 <= lists[i].length <= 500",
    "-10^4 <= lists[i][j] <= 10^4",
    "lists[i] 按 升序 排列",
    "lists[i].length 的总和不超过 10^4"
  ],
  "hints": [
    "可以使用分治法，两两合并链表",
    "优先队列也是一个好的选择",
    "复用合并两个有序链表的函数"
  ],
  "followUp": "你能想出更优化的解法吗？考虑时间和空间复杂度。",
  "solutions": [
    {
      "id": "divide_conquer",
      "name": "分治法",
      "timeComplexity": "O(N log k)",
      "spaceComplexity": "O(log k)",
      "description": "使用分治法递归合并链表",
      "code": {
        "javascript": "/**\n * 合并K个升序链表 - 使用分治法递归合并链表\n */\n\nfunction mergeKLists(lists) {\n    if (!lists || lists.length === 0) return null;\n\n    function mergeTwoLists(l1, l2) {\n        const dummy = { next: null };\n        let current = dummy;\n\n        while (l1 && l2) {\n            if (l1.val <= l2.val) {\n                current.next = l1;\n                l1 = l1.next;\n            } else {\n                current.next = l2;\n                l2 = l2.next;\n            }\n            current = current.next;\n        }\n\n        current.next = l1 || l2;\n        return dummy.next;\n    }\n\n    function divide(lists, start, end) {\n        if (start === end) return lists[start];\n        if (start > end) return null;\n\n        const mid = Math.floor((start + end) / 2);\n        const left = divide(lists, start, mid);\n        const right = divide(lists, mid + 1, end);\n\n        return mergeTwoLists(left, right);\n    }\n\n    return divide(lists, 0, lists.length - 1);\n}\n\n\n/**\n * 测试mergeKLists函数\n */\nfunction testMergeKLists() {\n    console.log(\"=== mergeKLists测试 ===\");\n\n    // 测试用例\n    console.log(\"输入: lists = [[1,4,5],[1,3,4],[2,6]]\");\n    console.log(\"期望输出: [1,1,2,3,4,4,5,6]\");\n    console.log(\"说明: 合并后的链表为: 1->1->2->3->4->4->5->6\");\n\n    console.log(\"\\n=== 测试完成 ===\");\n}\n\n// 如果需要运行测试，取消注释下面的行\n// testMergeKLists();",
        "python": "\"\"\"\n合并K个升序链表 - 使用分治法递归合并链表\n\"\"\"\n\ndef mergeKLists(lists):\n    if not lists:\n        return None\n\n    def merge_two_lists(l1, l2):\n        dummy = ListNode(0)\n        current = dummy\n\n        while l1 and l2:\n            if l1.val <= l2.val:\n                current.next = l1\n                l1 = l1.next\n            else:\n                current.next = l2\n                l2 = l2.next\n            current = current.next\n\n        current.next = l1 or l2\n        return dummy.next\n\n    def divide(lists, start, end):\n        if start == end:\n            return lists[start]\n        if start > end:\n            return None\n\n        mid = (start + end) // 2\n        left = divide(lists, start, mid)\n        right = divide(lists, mid + 1, end)\n\n        return merge_two_lists(left, right)\n\n    return divide(lists, 0, len(lists) - 1)\n\n\ndef test_mergeKLists():\n    \"\"\"测试mergeKLists函数\"\"\"\n    print(\"=== mergeKLists测试 ===\")\n\n    # 测试用例\n    print(\"输入: lists = [[1,4,5],[1,3,4],[2,6]]\")\n    print(\"期望输出: [1,1,2,3,4,4,5,6]\")\n    print(\"说明: 合并后的链表为: 1->1->2->3->4->4->5->6\")\n\n    print(\"\\n=== 测试完成 ===\")\n\n# 如果需要运行测试，取消注释下面的行\n# test_mergeKLists()"
      }
    }
  ],
  "aiExplanation": {
    "concept": {
      "title": "🧠 合并K个升序链表解题策略",
      "content": "这是一道经典的算法题目，重点学习解题思路和代码实现。"
    },
    "keyInsights": [
      "💰 理解题目的核心要求",
      "📊 选择合适的算法策略",
      "🔄 优化时间和空间复杂度"
    ],
    "commonMistakes": [
      "❌ 没有考虑边界条件",
      "❌ 算法实现有误",
      "❌ 忽略了题目的特殊要求"
    ]
  },
  "visualization": {
    "type": "step_by_step",
    "defaultData": {
      "input": "lists = [[1,4,5],[1,3,4],[2,6]]",
      "output": "[1,1,2,3,4,4,5,6]",
      "explanation": "合并后的链表为: 1->1->2->3->4->4->5->6"
    },
    "steps": [
      {
        "step": 1,
        "description": "分析输入数据",
        "highlight": [],
        "action": "开始算法执行"
      }
    ]
  },
  "relatedProblems": [],
  "practiceExercises": [
    {
      "title": "合并K个升序链表变种练习",
      "description": "基于合并K个升序链表的变种问题",
      "difficulty": "困难",
      "hints": [
        "考虑边界情况",
        "优化算法复杂度"
      ]
    }
  ]
}