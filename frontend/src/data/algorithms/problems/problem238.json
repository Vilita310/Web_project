{
  "meta": {
    "id": 238,
    "title": "除自身以外数组的乘积",
    "difficulty": "中等",
    "leetcodeId": 238,
    "category": "array",
    "patterns": [
      "array_prefix_suffix"
    ],
    "tags": [
      "数组",
      "前缀和"
    ],
    "estimatedTime": "25分钟",
    "passingRate": "50.0%",
    "version": "1.0",
    "lastUpdated": "2025-10-21"
  },
  "description": "给你一个整数数组 nums，返回 数组 answer ，其中 answer[i] 等于 nums 中除 nums[i] 之外其余各元素的乘积。题目数据 保证 数组 nums之中任意元素的全部前缀元素和后缀元素的乘积都在  32 位 整数范围内。请不要使用除法，且在 O(n) 时间复杂度内完成此题。",
  "examples": [
    {
      "input": "nums = [1,2,3,4]",
      "output": "[24,12,8,6]",
      "explanation": "除了自身以外其余各元素的乘积：[2*3*4, 1*3*4, 1*2*4, 1*2*3]"
    },
    {
      "input": "nums = [-1,1,0,-3,3]",
      "output": "[0,0,9,0,0]",
      "explanation": "包含0的情况下的乘积结果"
    }
  ],
  "constraints": [
    "2 <= nums.length <= 10⁵",
    "-30 <= nums[i] <= 30",
    "保证 数组 nums之中任意元素的全部前缀元素和后缀元素的乘积都在 32 位 整数范围内"
  ],
  "hints": [
    "试着从左到右遍历数组，对于数组中的每个元素，记录其左侧所有元素的乘积。",
    "然后从右到左遍历数组，对于数组中的每个元素，将其左侧所有元素的乘积与右侧所有元素的乘积相乘。"
  ],
  "followUp": "你能想出更优化的解法吗？考虑时间和空间复杂度。",
  "solutions": [
    {
      "id": "two_pass",
      "name": "左右乘积",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(1)",
      "description": "先计算左侧乘积，再计算右侧乘积",
      "code": {
        "javascript": "/**\n * 除自身以外数组的乘积 - 先计算左侧乘积，再计算右侧乘积\n */\n\nfunction productExceptSelf(nums) {\n    const result = new Array(nums.length);\n    \n    // 左侧乘积\n    result[0] = 1;\n    for (let i = 1; i < nums.length; i++) {\n        result[i] = result[i - 1] * nums[i - 1];\n    }\n    \n    // 右侧乘积\n    let rightProduct = 1;\n    for (let i = nums.length - 1; i >= 0; i--) {\n        result[i] *= rightProduct;\n        rightProduct *= nums[i];\n    }\n    \n    return result;\n}\n\n\n/**\n * 测试productExceptSelf函数\n */\nfunction testProductExceptSelf() {\n    console.log(\"=== productExceptSelf测试 ===\");\n\n    // 测试用例\n    console.log(\"输入: nums = [1,2,3,4]\");\n    console.log(\"期望输出: [24,12,8,6]\");\n    console.log(\"说明: 除了自身以外其余各元素的乘积：[2*3*4, 1*3*4, 1*2*4, 1*2*3]\");\n\n    console.log(\"\\n=== 测试完成 ===\");\n}\n\n// 如果需要运行测试，取消注释下面的行\n// testProductExceptSelf();",
        "python": "\"\"\"\n除自身以外数组的乘积 - 先计算左侧乘积，再计算右侧乘积\n\"\"\"\n\ndef productExceptSelf(nums):\n    result = [1] * len(nums)\n    \n    # 左侧乘积\n    for i in range(1, len(nums)):\n        result[i] = result[i - 1] * nums[i - 1]\n    \n    # 右侧乘积\n    right_product = 1\n    for i in range(len(nums) - 1, -1, -1):\n        result[i] *= right_product\n        right_product *= nums[i]\n    \n    return result\n\n\ndef test_productExceptSelf():\n    \"\"\"测试productExceptSelf函数\"\"\"\n    print(\"=== productExceptSelf测试 ===\")\n\n    # 测试用例\n    print(\"输入: nums = [1,2,3,4]\")\n    print(\"期望输出: [24,12,8,6]\")\n    print(\"说明: 除了自身以外其余各元素的乘积：[2*3*4, 1*3*4, 1*2*4, 1*2*3]\")\n\n    print(\"\\n=== 测试完成 ===\")\n\n# 如果需要运行测试，取消注释下面的行\n# test_productExceptSelf()"
      }
    }
  ],
  "aiExplanation": {
    "concept": {
      "title": "🧠 除自身以外数组的乘积解题策略",
      "content": "这是一道经典的算法题目，重点学习解题思路和代码实现。"
    },
    "keyInsights": [
      "💰 理解题目的核心要求",
      "📊 选择合适的算法策略",
      "🔄 优化时间和空间复杂度"
    ],
    "commonMistakes": [
      "❌ 没有考虑边界条件",
      "❌ 算法实现有误",
      "❌ 忽略了题目的特殊要求"
    ]
  },
  "visualization": {
    "type": "step_by_step",
    "defaultData": {
      "input": "nums = [1,2,3,4]",
      "output": "[24,12,8,6]",
      "explanation": "除了自身以外其余各元素的乘积：[2*3*4, 1*3*4, 1*2*4, 1*2*3]"
    },
    "steps": [
      {
        "step": 1,
        "description": "分析输入数据",
        "highlight": [],
        "action": "开始算法执行"
      }
    ]
  },
  "relatedProblems": [],
  "practiceExercises": [
    {
      "title": "除自身以外数组的乘积变种练习",
      "description": "基于除自身以外数组的乘积的变种问题",
      "difficulty": "中等",
      "hints": [
        "考虑边界情况",
        "优化算法复杂度"
      ]
    }
  ]
}