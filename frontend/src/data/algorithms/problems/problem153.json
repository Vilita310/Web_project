{
  "meta": {
    "id": 153,
    "title": "寻找旋转排序数组中的最小值",
    "difficulty": "中等",
    "leetcodeId": 153,
    "category": "array",
    "patterns": [
      "binary_search_min"
    ],
    "tags": [
      "数组",
      "二分查找"
    ],
    "estimatedTime": "25分钟",
    "passingRate": "50.0%",
    "version": "1.0",
    "lastUpdated": "2025-10-21"
  },
  "description": "已知一个长度为 n 的数组，预先按照升序排列，经由 1 到 n 次 旋转 后，得到输入数组。例如，原数组 nums = [0,1,2,4,5,6,7] 在变化后可能得到：若旋转 4 次，则可以得到 [4,5,6,7,0,1,2]；若旋转 7 次，则可以得到 [0,1,2,4,5,6,7]。注意，数组 [a[0], a[1], a[2], ..., a[n-1]] 旋转一次 的结果为数组 [a[n-1], a[0], a[1], a[2], ..., a[n-2]] 。给你一个元素值 互不相同 的数组 nums ，它原来是一个升序排列的数组，并按上述情形进行了多次旋转。请你找出并返回数组中的 最小元素 。你必须设计一个时间复杂度为 O(log n) 的算法解决此问题。",
  "examples": [
    {
      "input": "nums = [3,4,5,1,2]",
      "output": "1",
      "explanation": "原数组为 [1,2,3,4,5] ，旋转 3 次得到输入数组。"
    },
    {
      "input": "nums = [4,5,6,7,0,1,2]",
      "output": "0",
      "explanation": "原数组为 [0,1,2,4,5,6,7] ，旋转 4 次得到输入数组。"
    },
    {
      "input": "nums = [11,13,15,17]",
      "output": "11",
      "explanation": "原数组为 [11,13,15,17] ，旋转 4 次得到输入数组。"
    }
  ],
  "constraints": [
    "n == nums.length",
    "1 <= n <= 5000",
    "-5000 <= nums[i] <= 5000",
    "nums 中的所有整数 互不相同",
    "nums 原来是一个升序排序的数组，并进行了 1 至 n 次旋转"
  ],
  "hints": [
    "数组原本是升序的，因此最小值一定在旋转点。",
    "可以使用二分查找来定位旋转点。"
  ],
  "followUp": "你能想出更优化的解法吗？考虑时间和空间复杂度。",
  "solutions": [
    {
      "id": "binary_search",
      "name": "二分查找",
      "timeComplexity": "O(log n)",
      "spaceComplexity": "O(1)",
      "description": "使用二分查找找到旋转点，即最小值所在位置",
      "code": {
        "javascript": "/**\n * 寻找旋转排序数组中的最小值 - 使用二分查找找到旋转点，即最小值所在位置\n */\n\nfunction findMin(nums) {\n    let left = 0, right = nums.length - 1;\n    \n    while (left < right) {\n        const mid = Math.floor((left + right) / 2);\n        \n        if (nums[mid] > nums[right]) {\n            // 最小值在右半部分\n            left = mid + 1;\n        } else {\n            // 最小值在左半部分（包含mid）\n            right = mid;\n        }\n    }\n    \n    return nums[left];\n}\n\n\n/**\n * 测试findMin函数\n */\nfunction testFindMin() {\n    console.log(\"=== findMin测试 ===\");\n\n    // 测试用例\n    console.log(\"输入: nums = [3,4,5,1,2]\");\n    console.log(\"期望输出: 1\");\n    console.log(\"说明: 原数组为 [1,2,3,4,5] ，旋转 3 次得到输入数组。\");\n\n    console.log(\"\\n=== 测试完成 ===\");\n}\n\n// 如果需要运行测试，取消注释下面的行\n// testFindMin();",
        "python": "\"\"\"\n寻找旋转排序数组中的最小值 - 使用二分查找找到旋转点，即最小值所在位置\n\"\"\"\n\ndef findMin(nums):\n    left, right = 0, len(nums) - 1\n    \n    while left < right:\n        mid = (left + right) // 2\n        \n        if nums[mid] > nums[right]:\n            # 最小值在右半部分\n            left = mid + 1\n        else:\n            # 最小值在左半部分（包含mid）\n            right = mid\n    \n    return nums[left]\n\n\ndef test_findMin():\n    \"\"\"测试findMin函数\"\"\"\n    print(\"=== findMin测试 ===\")\n\n    # 测试用例\n    print(\"输入: nums = [3,4,5,1,2]\")\n    print(\"期望输出: 1\")\n    print(\"说明: 原数组为 [1,2,3,4,5] ，旋转 3 次得到输入数组。\")\n\n    print(\"\\n=== 测试完成 ===\")\n\n# 如果需要运行测试，取消注释下面的行\n# test_findMin()"
      }
    }
  ],
  "aiExplanation": {
    "concept": {
      "title": "🧠 寻找旋转排序数组中的最小值解题策略",
      "content": "这是一道经典的算法题目，重点学习解题思路和代码实现。"
    },
    "keyInsights": [
      "💰 理解题目的核心要求",
      "📊 选择合适的算法策略",
      "🔄 优化时间和空间复杂度"
    ],
    "commonMistakes": [
      "❌ 没有考虑边界条件",
      "❌ 算法实现有误",
      "❌ 忽略了题目的特殊要求"
    ]
  },
  "visualization": {
    "type": "step_by_step",
    "defaultData": {
      "input": "nums = [3,4,5,1,2]",
      "output": "1",
      "explanation": "原数组为 [1,2,3,4,5] ，旋转 3 次得到输入数组。"
    },
    "steps": [
      {
        "step": 1,
        "description": "分析输入数据",
        "highlight": [],
        "action": "开始算法执行"
      }
    ]
  },
  "relatedProblems": [],
  "practiceExercises": [
    {
      "title": "寻找旋转排序数组中的最小值变种练习",
      "description": "基于寻找旋转排序数组中的最小值的变种问题",
      "difficulty": "中等",
      "hints": [
        "考虑边界情况",
        "优化算法复杂度"
      ]
    }
  ]
}