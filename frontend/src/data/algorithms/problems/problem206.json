{
  "meta": {
    "id": 206,
    "title": "反转链表",
    "difficulty": "简单",
    "leetcodeId": 206,
    "category": "linked_list",
    "patterns": [
      "linked_list_basic_operations"
    ],
    "tags": [
      "链表",
      "递归"
    ],
    "estimatedTime": "15分钟",
    "passingRate": "50.0%",
    "version": "1.0",
    "lastUpdated": "2025-10-21"
  },
  "description": "给你单链表的头节点 head ，请你反转链表，并返回反转后的链表。",
  "examples": [
    {
      "input": "head = [1,2,3,4,5]",
      "output": "[5,4,3,2,1]",
      "explanation": "反转单链表"
    }
  ],
  "constraints": [
    "链表中节点的数目范围是 [0, 5000]",
    "-5000 <= Node.val <= 5000"
  ],
  "hints": [
    "递归版本稍微复杂一些，你能否用迭代的方法来解决这个问题？",
    "使用三个指针：prev、current、next"
  ],
  "followUp": "你能想出更优化的解法吗？考虑时间和空间复杂度。",
  "solutions": [
    {
      "id": "iterative",
      "name": "迭代解法",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(1)",
      "description": "使用三个指针迭代反转链表",
      "code": {
        "javascript": "/**\n * 链表节点定义\n */\nclass ListNode {\n    constructor(val, next) {\n        this.val = (val === undefined ? 0 : val);\n        this.next = (next === undefined ? null : next);\n    }\n}\n\n/**\n * 反转链表 - 迭代解法\n * @param {ListNode} head\n * @return {ListNode}\n */\nfunction reverseList(head) {\n    // 使用三个指针进行迭代反转\n    let prev = null;\n    let current = head;\n\n    while (current !== null) {\n        // 保存下一个节点\n        let nextTemp = current.next;\n\n        // 反转当前节点的指向\n        current.next = prev;\n\n        // 移动指针\n        prev = current;\n        current = nextTemp;\n    }\n\n    // prev现在指向新的头节点\n    return prev;\n}\n\n/**\n * 辅助函数：创建链表\n */\nfunction createLinkedList(arr) {\n    if (arr.length === 0) return null;\n\n    const head = new ListNode(arr[0]);\n    let current = head;\n\n    for (let i = 1; i < arr.length; i++) {\n        current.next = new ListNode(arr[i]);\n        current = current.next;\n    }\n\n    return head;\n}\n\n/**\n * 辅助函数：链表转数组\n */\nfunction linkedListToArray(head) {\n    const result = [];\n    let current = head;\n\n    while (current !== null) {\n        result.push(current.val);\n        current = current.next;\n    }\n\n    return result;\n}\n\n/**\n * 测试反转链表函数\n */\nfunction testReverseList() {\n    console.log(\"=== 反转链表测试 ===\");\n\n    // 测试用例1\n    const arr1 = [1, 2, 3, 4, 5];\n    const head1 = createLinkedList(arr1);\n    const reversed1 = reverseList(head1);\n    const result1 = linkedListToArray(reversed1);\n    console.log(`输入: [${arr1}]`);\n    console.log(`输出: [${result1}]`);\n    console.log(`期望: [5, 4, 3, 2, 1]`);\n    console.log(`结果: ${JSON.stringify(result1) === JSON.stringify([5, 4, 3, 2, 1]) ? '✅ 通过' : '❌ 失败'}`);\n\n    // 测试用例2\n    const arr2 = [1, 2];\n    const head2 = createLinkedList(arr2);\n    const reversed2 = reverseList(head2);\n    const result2 = linkedListToArray(reversed2);\n    console.log(`\\n输入: [${arr2}]`);\n    console.log(`输出: [${result2}]`);\n    console.log(`期望: [2, 1]`);\n    console.log(`结果: ${JSON.stringify(result2) === JSON.stringify([2, 1]) ? '✅ 通过' : '❌ 失败'}`);\n\n    // 测试用例3：空链表\n    const head3 = null;\n    const reversed3 = reverseList(head3);\n    const result3 = linkedListToArray(reversed3);\n    console.log(`\\n输入: []`);\n    console.log(`输出: [${result3}]`);\n    console.log(`期望: []`);\n    console.log(`结果: ${JSON.stringify(result3) === JSON.stringify([]) ? '✅ 通过' : '❌ 失败'}`);\n}\n\n// 如果需要运行测试，取消注释下面的行\n// testReverseList();",
        "python": "class ListNode:\n    \"\"\"链表节点定义\"\"\"\n    def __init__(self, val=0, next=None):\n        self.val = val\n        self.next = next\n\ndef reverseList(head):\n    \"\"\"\n    反转链表 - 迭代解法\n\n    Args:\n        head: 链表头节点\n\n    Returns:\n        反转后的链表头节点\n    \"\"\"\n    # 使用三个指针进行迭代反转\n    prev = None\n    current = head\n\n    while current:\n        # 保存下一个节点\n        next_temp = current.next\n\n        # 反转当前节点的指向\n        current.next = prev\n\n        # 移动指针\n        prev = current\n        current = next_temp\n\n    # prev现在指向新的头节点\n    return prev\n\ndef create_linked_list(arr):\n    \"\"\"辅助函数：创建链表\"\"\"\n    if not arr:\n        return None\n\n    head = ListNode(arr[0])\n    current = head\n\n    for i in range(1, len(arr)):\n        current.next = ListNode(arr[i])\n        current = current.next\n\n    return head\n\ndef linked_list_to_array(head):\n    \"\"\"辅助函数：链表转数组\"\"\"\n    result = []\n    current = head\n\n    while current:\n        result.append(current.val)\n        current = current.next\n\n    return result\n\ndef test_reverse_list():\n    \"\"\"测试反转链表函数\"\"\"\n    print(\"=== 反转链表测试 ===\")\n\n    # 测试用例1\n    arr1 = [1, 2, 3, 4, 5]\n    head1 = create_linked_list(arr1)\n    reversed1 = reverseList(head1)\n    result1 = linked_list_to_array(reversed1)\n    print(f\"输入: {arr1}\")\n    print(f\"输出: {result1}\")\n    print(f\"期望: [5, 4, 3, 2, 1]\")\n    print(f\"结果: {'✅ 通过' if result1 == [5, 4, 3, 2, 1] else '❌ 失败'}\")\n\n    # 测试用例2\n    arr2 = [1, 2]\n    head2 = create_linked_list(arr2)\n    reversed2 = reverseList(head2)\n    result2 = linked_list_to_array(reversed2)\n    print(f\"\\n输入: {arr2}\")\n    print(f\"输出: {result2}\")\n    print(f\"期望: [2, 1]\")\n    print(f\"结果: {'✅ 通过' if result2 == [2, 1] else '❌ 失败'}\")\n\n    # 测试用例3：空链表\n    head3 = None\n    reversed3 = reverseList(head3)\n    result3 = linked_list_to_array(reversed3)\n    print(f\"\\n输入: []\")\n    print(f\"输出: {result3}\")\n    print(f\"期望: []\")\n    print(f\"结果: {'✅ 通过' if result3 == [] else '❌ 失败'}\")\n\n# 如果需要运行测试，取消注释下面的行\n# test_reverse_list()"
      }
    }
  ],
  "aiExplanation": {
    "concept": {
      "title": "🧠 反转链表解题策略",
      "content": "这是一道经典的算法题目，重点学习解题思路和代码实现。"
    },
    "keyInsights": [
      "💰 理解题目的核心要求",
      "📊 选择合适的算法策略",
      "🔄 优化时间和空间复杂度"
    ],
    "commonMistakes": [
      "❌ 没有考虑边界条件",
      "❌ 算法实现有误",
      "❌ 忽略了题目的特殊要求"
    ]
  },
  "visualization": {
    "type": "step_by_step",
    "defaultData": {
      "input": "head = [1,2,3,4,5]",
      "output": "[5,4,3,2,1]",
      "explanation": "反转单链表"
    },
    "steps": [
      {
        "step": 1,
        "description": "分析输入数据",
        "highlight": [],
        "action": "开始算法执行"
      }
    ]
  },
  "relatedProblems": [],
  "practiceExercises": [
    {
      "title": "反转链表变种练习",
      "description": "基于反转链表的变种问题",
      "difficulty": "简单",
      "hints": [
        "考虑边界情况",
        "优化算法复杂度"
      ]
    }
  ]
}