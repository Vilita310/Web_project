{
  "meta": {
    "id": 20,
    "title": "有效的括号",
    "difficulty": "简单",
    "leetcodeId": 20,
    "category": "stack",
    "patterns": [
      "stack_basic"
    ],
    "tags": [
      "栈",
      "字符串"
    ],
    "estimatedTime": "15分钟",
    "passingRate": "50.0%",
    "version": "1.0",
    "lastUpdated": "2025-10-21"
  },
  "description": "给定一个只包括 '('，')'，'{'，'}'，'['，']' 的字符串 s ，判断字符串是否有效。有效字符串需满足：左括号必须用相同类型的右括号闭合。左括号必须以正确的顺序闭合。",
  "examples": [
    {
      "input": "s = \"()\"",
      "output": "true",
      "explanation": "括号匹配"
    },
    {
      "input": "s = \"()[]{}\"",
      "output": "true",
      "explanation": "多种括号匹配"
    },
    {
      "input": "s = \"(]\"",
      "output": "false",
      "explanation": "括号类型不匹配"
    }
  ],
  "constraints": [
    "1 <= s.length <= 10⁴",
    "s 仅由括号 '()[]{}' 组成"
  ],
  "hints": [
    "使用栈来匹配括号",
    "遇到左括号入栈，遇到右括号时检查栈顶是否为对应的左括号"
  ],
  "followUp": "你能想出更优化的解法吗？考虑时间和空间复杂度。",
  "solutions": [
    {
      "id": "stack",
      "name": "栈",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(n)",
      "description": "使用栈来匹配括号对",
      "code": {
        "javascript": "/**\n * 有效的括号 - 使用栈来匹配括号对\n */\n\nfunction isValid(s) {\n    const stack = [];\n    const map = { ')': '(', '}': '{', ']': '[' };\n    for (let char of s) {\n        if (char in map) {\n            if (stack.pop() !== map[char]) return false;\n        } else {\n            stack.push(char);\n        }\n    }\n    return stack.length === 0;\n}\n\n\n/**\n * 测试isValid函数\n */\nfunction testIsValid() {\n    console.log(\"=== isValid测试 ===\");\n\n    // 测试用例\n    console.log(\"输入: s = \"()\"\");\n    console.log(\"期望输出: true\");\n    console.log(\"说明: 括号匹配\");\n\n    console.log(\"\\n=== 测试完成 ===\");\n}\n\n// 如果需要运行测试，取消注释下面的行\n// testIsValid();",
        "python": "\"\"\"\n有效的括号 - 使用栈来匹配括号对\n\"\"\"\n\ndef isValid(s):\n    stack = []\n    mapping = {')': '(', '}': '{', ']': '['}\n    for char in s:\n        if char in mapping:\n            if not stack or stack.pop() != mapping[char]:\n                return False\n        else:\n            stack.append(char)\n    return len(stack) == 0\n\n\ndef test_isValid():\n    \"\"\"测试isValid函数\"\"\"\n    print(\"=== isValid测试 ===\")\n\n    # 测试用例\n    print(\"输入: s = \"()\"\")\n    print(\"期望输出: true\")\n    print(\"说明: 括号匹配\")\n\n    print(\"\\n=== 测试完成 ===\")\n\n# 如果需要运行测试，取消注释下面的行\n# test_isValid()"
      }
    }
  ],
  "aiExplanation": {
    "concept": {
      "title": "🧠 有效的括号解题策略",
      "content": "这是一道经典的算法题目，重点学习解题思路和代码实现。"
    },
    "keyInsights": [
      "💰 理解题目的核心要求",
      "📊 选择合适的算法策略",
      "🔄 优化时间和空间复杂度"
    ],
    "commonMistakes": [
      "❌ 没有考虑边界条件",
      "❌ 算法实现有误",
      "❌ 忽略了题目的特殊要求"
    ]
  },
  "visualization": {
    "type": "step_by_step",
    "defaultData": {
      "input": "s = \"()\"",
      "output": "true",
      "explanation": "括号匹配"
    },
    "steps": [
      {
        "step": 1,
        "description": "分析输入数据",
        "highlight": [],
        "action": "开始算法执行"
      }
    ]
  },
  "relatedProblems": [],
  "practiceExercises": [
    {
      "title": "有效的括号变种练习",
      "description": "基于有效的括号的变种问题",
      "difficulty": "简单",
      "hints": [
        "考虑边界情况",
        "优化算法复杂度"
      ]
    }
  ]
}