{
  "meta": {
    "id": 15,
    "title": "三数之和",
    "difficulty": "中等",
    "leetcodeId": 15,
    "category": "array",
    "patterns": [
      "array_two_pointers"
    ],
    "tags": [
      "数组",
      "双指针",
      "排序"
    ],
    "estimatedTime": "25分钟",
    "passingRate": "50.0%",
    "version": "1.0",
    "lastUpdated": "2025-10-21"
  },
  "description": "给你一个包含 n 个整数的数组 nums，判断 nums 中是否存在三个元素 a，b，c ，使得 a + b + c = 0 ？请你找出所有和为 0 且不重复的三元组。注意：答案中不可以包含重复的三元组。",
  "examples": [
    {
      "input": "nums = [-1,0,1,2,-1,-4]",
      "output": "[[-1,-1,2],[-1,0,1]]",
      "explanation": "注意，输出的顺序和三元组的顺序并不重要。"
    }
  ],
  "constraints": [
    "0 <= nums.length <= 3000",
    "-10⁵ <= nums[i] <= 10⁵"
  ],
  "hints": [
    "先将数组排序",
    "固定一个数，使用双指针寻找另外两个数",
    "注意去重处理"
  ],
  "followUp": "你能想出更优化的解法吗？考虑时间和空间复杂度。",
  "solutions": [
    {
      "id": "sort_two_pointers",
      "name": "排序+双指针",
      "timeComplexity": "O(n²)",
      "spaceComplexity": "O(1)",
      "description": "先排序，然后固定一个数，用双指针找另外两个数",
      "code": {
        "javascript": "/**\n * 三数之和 - 先排序，然后固定一个数，用双指针找另外两个数\n */\n\nfunction threeSum(nums) {\n    const result = [];\n    nums.sort((a, b) => a - b);\n    for (let i = 0; i < nums.length - 2; i++) {\n        if (i > 0 && nums[i] === nums[i - 1]) continue;\n        let left = i + 1, right = nums.length - 1;\n        while (left < right) {\n            const sum = nums[i] + nums[left] + nums[right];\n            if (sum === 0) {\n                result.push([nums[i], nums[left], nums[right]]);\n                while (left < right && nums[left] === nums[left + 1]) left++;\n                while (left < right && nums[right] === nums[right - 1]) right--;\n                left++; right--;\n            } else if (sum < 0) left++;\n            else right--;\n        }\n    }\n    return result;\n}\n\n\n/**\n * 测试threeSum函数\n */\nfunction testThreeSum() {\n    console.log(\"=== threeSum测试 ===\");\n\n    // 测试用例\n    console.log(\"输入: nums = [-1,0,1,2,-1,-4]\");\n    console.log(\"期望输出: [[-1,-1,2],[-1,0,1]]\");\n    console.log(\"说明: 注意，输出的顺序和三元组的顺序并不重要。\");\n\n    console.log(\"\\n=== 测试完成 ===\");\n}\n\n// 如果需要运行测试，取消注释下面的行\n// testThreeSum();",
        "python": "\"\"\"\n三数之和 - 先排序，然后固定一个数，用双指针找另外两个数\n\"\"\"\n\ndef threeSum(nums):\n    result = []\n    nums.sort()\n    for i in range(len(nums) - 2):\n        if i > 0 and nums[i] == nums[i - 1]:\n            continue\n        left, right = i + 1, len(nums) - 1\n        while left < right:\n            current_sum = nums[i] + nums[left] + nums[right]\n            if current_sum == 0:\n                result.append([nums[i], nums[left], nums[right]])\n                while left < right and nums[left] == nums[left + 1]:\n                    left += 1\n                while left < right and nums[right] == nums[right - 1]:\n                    right -= 1\n                left += 1\n                right -= 1\n            elif current_sum < 0:\n                left += 1\n            else:\n                right -= 1\n    return result\n\n\ndef test_threeSum():\n    \"\"\"测试threeSum函数\"\"\"\n    print(\"=== threeSum测试 ===\")\n\n    # 测试用例\n    print(\"输入: nums = [-1,0,1,2,-1,-4]\")\n    print(\"期望输出: [[-1,-1,2],[-1,0,1]]\")\n    print(\"说明: 注意，输出的顺序和三元组的顺序并不重要。\")\n\n    print(\"\\n=== 测试完成 ===\")\n\n# 如果需要运行测试，取消注释下面的行\n# test_threeSum()"
      }
    }
  ],
  "aiExplanation": {
    "concept": {
      "title": "🧠 三数之和解题策略",
      "content": "这是一道经典的算法题目，重点学习解题思路和代码实现。"
    },
    "keyInsights": [
      "💰 理解题目的核心要求",
      "📊 选择合适的算法策略",
      "🔄 优化时间和空间复杂度"
    ],
    "commonMistakes": [
      "❌ 没有考虑边界条件",
      "❌ 算法实现有误",
      "❌ 忽略了题目的特殊要求"
    ]
  },
  "visualization": {
    "type": "step_by_step",
    "defaultData": {
      "input": "nums = [-1,0,1,2,-1,-4]",
      "output": "[[-1,-1,2],[-1,0,1]]",
      "explanation": "注意，输出的顺序和三元组的顺序并不重要。"
    },
    "steps": [
      {
        "step": 1,
        "description": "分析输入数据",
        "highlight": [],
        "action": "开始算法执行"
      }
    ]
  },
  "relatedProblems": [],
  "practiceExercises": [
    {
      "title": "三数之和变种练习",
      "description": "基于三数之和的变种问题",
      "difficulty": "中等",
      "hints": [
        "考虑边界情况",
        "优化算法复杂度"
      ]
    }
  ]
}