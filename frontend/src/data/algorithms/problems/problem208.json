{
  "meta": {
    "id": 208,
    "title": "实现 Trie (前缀树)",
    "difficulty": "中等",
    "leetcodeId": 208,
    "category": "trie",
    "patterns": [
      "trie_implementation"
    ],
    "tags": [
      "设计",
      "字典树",
      "哈希表",
      "字符串"
    ],
    "estimatedTime": "25分钟",
    "passingRate": "50.0%",
    "version": "1.0",
    "lastUpdated": "2025-10-21"
  },
  "description": "Trie（发音类似 \"try\"）或者说 前缀树 是一种树形数据结构，用于高效地存储和检索字符串数据集中的键。这一数据结构有相当多的应用情景，例如自动补全和拼写检查。请你实现 Trie 类：Trie() 初始化前缀树对象。void insert(String word) 向前缀树中插入字符串 word 。boolean search(String word) 返回前缀树中是否存在字符串 word 。boolean startsWith(String prefix) 返回前缀树中是否存在以 prefix 为前缀的字符串。",
  "examples": [
    {
      "input": "[\"Trie\", \"insert\", \"search\", \"search\", \"startsWith\", \"insert\", \"search\"]\n[[], [\"apple\"], [\"apple\"], [\"app\"], [\"app\"], [\"app\"], [\"app\"]]",
      "output": "[null, null, true, false, true, null, true]",
      "explanation": "Trie trie = new Trie();\ntrie.insert(\"apple\");\ntrie.search(\"apple\");   // 返回 True\ntrie.search(\"app\");     // 返回 False\ntrie.startsWith(\"app\"); // 返回 True\ntrie.insert(\"app\");\ntrie.search(\"app\");     // 返回 True"
    }
  ],
  "constraints": [
    "1 <= word.length, prefix.length <= 2000",
    "word 和 prefix 仅由小写英文字母组成",
    "insert、search 和 startsWith 调用次数 总计 不超过 3 * 10^4 次"
  ],
  "hints": [
    "使用树形结构，每个节点包含子节点数组",
    "每个节点标记是否为单词结尾",
    "遍历字符串建立路径"
  ],
  "followUp": "你能想出更优化的解法吗？考虑时间和空间复杂度。",
  "solutions": [
    {
      "id": "trie_tree",
      "name": "前缀树",
      "timeComplexity": "O(m)",
      "spaceComplexity": "O(ALPHABET_SIZE * N * M)",
      "description": "使用树形结构实现前缀树",
      "code": {
        "javascript": "/**\n * 实现 Trie (前缀树)\n */\n\n/**\n * Trie节点类\n */\nclass TrieNode {\n    constructor() {\n        this.children = {}; // 存储子节点\n        this.isEndOfWord = false; // 标记是否为单词结尾\n    }\n}\n\n/**\n * Trie前缀树类\n */\nclass Trie {\n    constructor() {\n        this.root = new TrieNode(); // 根节点\n    }\n\n    /**\n     * 向前缀树中插入字符串\n     * @param {string} word\n     */\n    insert(word) {\n        let node = this.root;\n        // 遍历单词的每个字符\n        for (const char of word) {\n            // 如果字符不存在，创建新节点\n            if (!node.children[char]) {\n                node.children[char] = new TrieNode();\n            }\n            node = node.children[char];\n        }\n        // 标记单词结尾\n        node.isEndOfWord = true;\n    }\n\n    /**\n     * 搜索字符串是否存在\n     * @param {string} word\n     * @return {boolean}\n     */\n    search(word) {\n        let node = this.root;\n        // 遍历单词的每个字符\n        for (const char of word) {\n            if (!node.children[char]) {\n                return false; // 字符不存在\n            }\n            node = node.children[char];\n        }\n        // 必须是完整单词\n        return node.isEndOfWord;\n    }\n\n    /**\n     * 检查是否存在以给定前缀开头的单词\n     * @param {string} prefix\n     * @return {boolean}\n     */\n    startsWith(prefix) {\n        let node = this.root;\n        // 遍历前缀的每个字符\n        for (const char of prefix) {\n            if (!node.children[char]) {\n                return false; // 前缀不存在\n            }\n            node = node.children[char];\n        }\n        return true; // 前缀存在\n    }\n}\n\n/**\n * 测试Trie前缀树\n */\nfunction testTrie() {\n    console.log(\"=== Trie前缀树测试 ===\");\n\n    const trie = new Trie();\n    trie.insert(\"apple\");\n    const result = trie.search(\"apple\");\n    console.log(`搜索 \"apple\": ${result} (期望: true)`);\n    console.log(`结果: ${result === true ? '✅ 通过' : '❌ 失败'}`);\n\n    console.log(\"\\n=== 测试完成 ===\");\n}\n\n// 如果需要运行测试，取消注释下面的行\n// testTrie();",
        "python": "\"\"\"\n实现 Trie (前缀树)\n\"\"\"\n\nclass TrieNode:\n    \"\"\"Trie节点类\"\"\"\n    def __init__(self):\n        self.children = {}  # 存储子节点\n        self.is_end_of_word = False  # 标记是否为单词结尾\n\nclass Trie:\n    \"\"\"Trie前缀树类\"\"\"\n    def __init__(self):\n        \"\"\"初始化前缀树\"\"\"\n        self.root = TrieNode()  # 根节点\n\n    def insert(self, word):\n        \"\"\"向前缀树中插入字符串\"\"\"\n        node = self.root\n        # 遍历单词的每个字符\n        for char in word:\n            # 如果字符不存在，创建新节点\n            if char not in node.children:\n                node.children[char] = TrieNode()\n            node = node.children[char]\n        # 标记单词结尾\n        node.is_end_of_word = True\n\n    def search(self, word):\n        \"\"\"搜索字符串是否存在\"\"\"\n        node = self.root\n        # 遍历单词的每个字符\n        for char in word:\n            if char not in node.children:\n                return False  # 字符不存在\n            node = node.children[char]\n        # 必须是完整单词\n        return node.is_end_of_word\n\n    def startsWith(self, prefix):\n        \"\"\"检查是否存在以给定前缀开头的单词\"\"\"\n        node = self.root\n        # 遍历前缀的每个字符\n        for char in prefix:\n            if char not in node.children:\n                return False  # 前缀不存在\n            node = node.children[char]\n        return True  # 前缀存在\n\ndef test_trie():\n    \"\"\"测试Trie前缀树\"\"\"\n    print(\"=== Trie前缀树测试 ===\")\n    \n    trie = Trie()\n    trie.insert(\"apple\")\n    result = trie.search(\"apple\")\n    print(f\"搜索 'apple': {result} (期望: True)\")\n    print(f\"结果: {'✅ 通过' if result == True else '❌ 失败'}\")\n    \n    print(\"\\n=== 测试完成 ===\")\n\n# 如果需要运行测试，取消注释下面的行\n# test_trie()"
      }
    }
  ],
  "aiExplanation": {
    "concept": {
      "title": "🧠 实现 Trie (前缀树)解题策略",
      "content": "这是一道经典的算法题目，重点学习解题思路和代码实现。"
    },
    "keyInsights": [
      "💰 理解题目的核心要求",
      "📊 选择合适的算法策略",
      "🔄 优化时间和空间复杂度"
    ],
    "commonMistakes": [
      "❌ 没有考虑边界条件",
      "❌ 算法实现有误",
      "❌ 忽略了题目的特殊要求"
    ]
  },
  "visualization": {
    "type": "step_by_step",
    "defaultData": {
      "input": "[\"Trie\", \"insert\", \"search\", \"search\", \"startsWith\", \"insert\", \"search\"]\n[[], [\"apple\"], [\"apple\"], [\"app\"], [\"app\"], [\"app\"], [\"app\"]]",
      "output": "[null, null, true, false, true, null, true]",
      "explanation": "Trie trie = new Trie();\ntrie.insert(\"apple\");\ntrie.search(\"apple\");   // 返回 True\ntrie.search(\"app\");     // 返回 False\ntrie.startsWith(\"app\"); // 返回 True\ntrie.insert(\"app\");\ntrie.search(\"app\");     // 返回 True"
    },
    "steps": [
      {
        "step": 1,
        "description": "分析输入数据",
        "highlight": [],
        "action": "开始算法执行"
      }
    ]
  },
  "relatedProblems": [],
  "practiceExercises": [
    {
      "title": "实现 Trie (前缀树)变种练习",
      "description": "基于实现 Trie (前缀树)的变种问题",
      "difficulty": "中等",
      "hints": [
        "考虑边界情况",
        "优化算法复杂度"
      ]
    }
  ]
}