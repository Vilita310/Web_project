{
  "meta": {
    "id": 100,
    "title": "相同的树",
    "difficulty": "简单",
    "leetcodeId": 100,
    "category": "tree",
    "patterns": [
      "tree_traversal"
    ],
    "tags": [
      "树",
      "深度优先搜索",
      "广度优先搜索"
    ],
    "estimatedTime": "15分钟",
    "passingRate": "50.0%",
    "version": "1.0",
    "lastUpdated": "2025-10-21"
  },
  "description": "给你两棵二叉树的根节点 p 和 q ，编写一个函数来检验这两棵树是否相同。如果两个树在结构上相同，并且节点具有相同的值，则认为它们是相同的。",
  "examples": [
    {
      "input": "p = [1,2,3], q = [1,2,3]",
      "output": "true",
      "explanation": "两棵树完全相同。"
    },
    {
      "input": "p = [1,2], q = [1,null,2]",
      "output": "false",
      "explanation": "树的结构不同。"
    },
    {
      "input": "p = [1,2,1], q = [1,1,2]",
      "output": "false",
      "explanation": "节点值不同。"
    }
  ],
  "constraints": [
    "两棵树上的节点数目都在范围 [0, 100] 内",
    "-10⁴ <= Node.val <= 10⁴"
  ],
  "hints": [
    "可以使用递归的方法。",
    "同时遍历两棵树，比较对应节点的值。"
  ],
  "followUp": "你能想出更优化的解法吗？考虑时间和空间复杂度。",
  "solutions": [
    {
      "id": "recursive",
      "name": "递归",
      "timeComplexity": "O(min(m,n))",
      "spaceComplexity": "O(min(m,n))",
      "description": "递归比较两棵树的每个节点",
      "code": {
        "javascript": "/**\n * 相同的树 - 递归比较两棵树的每个节点\n */\n\nfunction isSameTree(p, q) {\n    // 如果两个节点都为空，则相同\n    if (!p && !q) return true;\n    \n    // 如果一个为空一个不为空，则不同\n    if (!p || !q) return false;\n    \n    // 如果节点值不同，则不同\n    if (p.val !== q.val) return false;\n    \n    // 递归比较左右子树\n    return isSameTree(p.left, q.left) && isSameTree(p.right, q.right);\n}\n\n\n/**\n * 测试isSameTree函数\n */\nfunction testIsSameTree() {\n    console.log(\"=== isSameTree测试 ===\");\n\n    // 测试用例\n    console.log(\"输入: p = [1,2,3], q = [1,2,3]\");\n    console.log(\"期望输出: true\");\n    console.log(\"说明: 两棵树完全相同。\");\n\n    console.log(\"\\n=== 测试完成 ===\");\n}\n\n// 如果需要运行测试，取消注释下面的行\n// testIsSameTree();",
        "python": "\"\"\"\n相同的树 - 递归比较两棵树的每个节点\n\"\"\"\n\ndef isSameTree(p, q):\n    # 如果两个节点都为空，则相同\n    if not p and not q:\n        return True\n    \n    # 如果一个为空一个不为空，则不同\n    if not p or not q:\n        return False\n    \n    # 如果节点值不同，则不同\n    if p.val != q.val:\n        return False\n    \n    # 递归比较左右子树\n    return isSameTree(p.left, q.left) and isSameTree(p.right, q.right)\n\n\ndef test_isSameTree():\n    \"\"\"测试isSameTree函数\"\"\"\n    print(\"=== isSameTree测试 ===\")\n\n    # 测试用例\n    print(\"输入: p = [1,2,3], q = [1,2,3]\")\n    print(\"期望输出: true\")\n    print(\"说明: 两棵树完全相同。\")\n\n    print(\"\\n=== 测试完成 ===\")\n\n# 如果需要运行测试，取消注释下面的行\n# test_isSameTree()"
      }
    }
  ],
  "aiExplanation": {
    "concept": {
      "title": "🧠 相同的树解题策略",
      "content": "这是一道经典的算法题目，重点学习解题思路和代码实现。"
    },
    "keyInsights": [
      "💰 理解题目的核心要求",
      "📊 选择合适的算法策略",
      "🔄 优化时间和空间复杂度"
    ],
    "commonMistakes": [
      "❌ 没有考虑边界条件",
      "❌ 算法实现有误",
      "❌ 忽略了题目的特殊要求"
    ]
  },
  "visualization": {
    "type": "step_by_step",
    "defaultData": {
      "input": "p = [1,2,3], q = [1,2,3]",
      "output": "true",
      "explanation": "两棵树完全相同。"
    },
    "steps": [
      {
        "step": 1,
        "description": "分析输入数据",
        "highlight": [],
        "action": "开始算法执行"
      }
    ]
  },
  "relatedProblems": [],
  "practiceExercises": [
    {
      "title": "相同的树变种练习",
      "description": "基于相同的树的变种问题",
      "difficulty": "简单",
      "hints": [
        "考虑边界情况",
        "优化算法复杂度"
      ]
    }
  ]
}