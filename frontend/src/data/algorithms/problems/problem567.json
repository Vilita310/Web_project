{
  "meta": {
    "id": 567,
    "title": "字符串的排列",
    "difficulty": "中等",
    "leetcodeId": 567,
    "category": "sliding_window",
    "patterns": [
      "sliding_window_permutation"
    ],
    "tags": [
      "哈希表",
      "双指针",
      "字符串",
      "滑动窗口"
    ],
    "estimatedTime": "25分钟",
    "passingRate": "50.0%",
    "version": "1.0",
    "lastUpdated": "2025-10-21"
  },
  "description": "给你两个字符串 s1 和 s2 ，写一个函数来判断 s2 是否包含 s1 的排列。如果是，返回 true ；否则，返回 false 。换句话说，s1 的排列之一是 s2 的 子串 。",
  "examples": [
    {
      "input": "s1 = \"ab\", s2 = \"eidbaooo\"",
      "output": "true",
      "explanation": "s2 包含 s1 的排列之一 (\"ba\")."
    },
    {
      "input": "s1 = \"ab\", s2 = \"eidboaoo\"",
      "output": "false",
      "explanation": "s2 不包含 s1 的排列。"
    }
  ],
  "constraints": [
    "1 <= s1.length, s2.length <= 10⁴",
    "s1 和 s2 仅包含小写字母"
  ],
  "hints": [
    "使用滑动窗口技术",
    "维护窗口内字符的频次",
    "比较窗口字符频次与s1的字符频次"
  ],
  "followUp": "你能想出更优化的解法吗？考虑时间和空间复杂度。",
  "solutions": [
    {
      "id": "sliding_window",
      "name": "滑动窗口",
      "timeComplexity": "O(n + m)",
      "spaceComplexity": "O(26) = O(1)",
      "description": "使用滑动窗口检查排列",
      "code": {
        "javascript": "/**\n * 字符串的排列 - 使用滑动窗口检查排列\n */\n\nfunction checkInclusion(s1, s2) {\n    if (s1.length > s2.length) return false;\n\n    // 统计s1中字符的频次\n    const s1Count = new Array(26).fill(0);\n    for (const char of s1) {\n        s1Count[char.charCodeAt(0) - 97]++; // 'a' = 97\n    }\n\n    // 滑动窗口\n    const windowCount = new Array(26).fill(0);\n    const windowSize = s1.length;\n\n    for (let i = 0; i < s2.length; i++) {\n        // 添加右边字符\n        const rightChar = s2.charCodeAt(i) - 97;\n        windowCount[rightChar]++;\n\n        // 如果窗口大小超过s1长度，移除左边字符\n        if (i >= windowSize) {\n            const leftChar = s2.charCodeAt(i - windowSize) - 97;\n            windowCount[leftChar]--;\n        }\n\n        // 检查窗口是否匹配\n        if (i >= windowSize - 1) {\n            if (arraysEqual(s1Count, windowCount)) {\n                return true;\n            }\n        }\n    }\n\n    return false;\n\n    function arraysEqual(arr1, arr2) {\n        for (let i = 0; i < arr1.length; i++) {\n            if (arr1[i] !== arr2[i]) return false;\n        }\n        return true;\n    }\n}\n\n\n/**\n * 测试checkInclusion函数\n */\nfunction testCheckInclusion() {\n    console.log(\"=== checkInclusion测试 ===\");\n\n    // 测试用例\n    console.log(\"输入: s1 = \"ab\", s2 = \"eidbaooo\"\");\n    console.log(\"期望输出: true\");\n    console.log(\"说明: s2 包含 s1 的排列之一 (\"ba\").\");\n\n    console.log(\"\\n=== 测试完成 ===\");\n}\n\n// 如果需要运行测试，取消注释下面的行\n// testCheckInclusion();",
        "python": "\"\"\"\n字符串的排列 - 使用滑动窗口检查排列\n\"\"\"\n\ndef checkInclusion(s1, s2):\n    if len(s1) > len(s2):\n        return False\n\n    # 统计s1中字符的频次\n    s1_count = [0] * 26\n    for char in s1:\n        s1_count[ord(char) - ord('a')] += 1\n\n    # 滑动窗口\n    window_count = [0] * 26\n    window_size = len(s1)\n\n    for i in range(len(s2)):\n        # 添加右边字符\n        right_char = ord(s2[i]) - ord('a')\n        window_count[right_char] += 1\n\n        # 如果窗口大小超过s1长度，移除左边字符\n        if i >= window_size:\n            left_char = ord(s2[i - window_size]) - ord('a')\n            window_count[left_char] -= 1\n\n        # 检查窗口是否匹配\n        if i >= window_size - 1:\n            if s1_count == window_count:\n                return True\n\n    return False\n\n\ndef test_checkInclusion():\n    \"\"\"测试checkInclusion函数\"\"\"\n    print(\"=== checkInclusion测试 ===\")\n\n    # 测试用例\n    print(\"输入: s1 = \"ab\", s2 = \"eidbaooo\"\")\n    print(\"期望输出: true\")\n    print(\"说明: s2 包含 s1 的排列之一 (\"ba\").\")\n\n    print(\"\\n=== 测试完成 ===\")\n\n# 如果需要运行测试，取消注释下面的行\n# test_checkInclusion()"
      }
    }
  ],
  "aiExplanation": {
    "concept": {
      "title": "🧠 字符串的排列解题策略",
      "content": "这是一道经典的算法题目，重点学习解题思路和代码实现。"
    },
    "keyInsights": [
      "💰 理解题目的核心要求",
      "📊 选择合适的算法策略",
      "🔄 优化时间和空间复杂度"
    ],
    "commonMistakes": [
      "❌ 没有考虑边界条件",
      "❌ 算法实现有误",
      "❌ 忽略了题目的特殊要求"
    ]
  },
  "visualization": {
    "type": "step_by_step",
    "defaultData": {
      "input": "s1 = \"ab\", s2 = \"eidbaooo\"",
      "output": "true",
      "explanation": "s2 包含 s1 的排列之一 (\"ba\")."
    },
    "steps": [
      {
        "step": 1,
        "description": "分析输入数据",
        "highlight": [],
        "action": "开始算法执行"
      }
    ]
  },
  "relatedProblems": [],
  "practiceExercises": [
    {
      "title": "字符串的排列变种练习",
      "description": "基于字符串的排列的变种问题",
      "difficulty": "中等",
      "hints": [
        "考虑边界情况",
        "优化算法复杂度"
      ]
    }
  ]
}