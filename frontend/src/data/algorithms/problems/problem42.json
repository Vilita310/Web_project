{
  "meta": {
    "id": 42,
    "title": "接雨水",
    "difficulty": "困难",
    "leetcodeId": 42,
    "category": "array",
    "patterns": [
      "array_two_pointers"
    ],
    "tags": [
      "栈",
      "数组",
      "双指针"
    ],
    "estimatedTime": "45分钟",
    "passingRate": "50.0%",
    "version": "1.0",
    "lastUpdated": "2025-10-21"
  },
  "description": "给定 n 个非负整数表示每个宽度为 1 的柱子的高度图，计算按此排列的柱子，下雨之后能够接多少雨水。",
  "examples": [
    {
      "input": "height = [0,1,0,2,1,0,1,3,2,1,2,1]",
      "output": "6",
      "explanation": "上面是由数组 [0,1,0,2,1,0,1,3,2,1,2,1] 表示的高度图，在这种情况下，可以接 6 个单位的雨水（蓝色部分表示雨水）。"
    }
  ],
  "constraints": [
    "n == height.length",
    "1 <= n <= 2 * 10⁴",
    "0 <= height[i] <= 3 * 10⁴"
  ],
  "hints": [
    "对于数组中的每个元素，我们找出下雨后水能达到的最高位置，等于两边最大高度的较小值减去当前高度的值",
    "使用双指针维护一个最大高度"
  ],
  "followUp": "你能想出更优化的解法吗？考虑时间和空间复杂度。",
  "solutions": [
    {
      "id": "two_pointers",
      "name": "双指针",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(1)",
      "description": "使用双指针和两个变量来记录左右两边的最大高度",
      "code": {
        "javascript": "/**\n * 接雨水 - 使用双指针和两个变量来记录左右两边的最大高度\n */\n\nfunction trap(height) {\n    let left = 0, right = height.length - 1;\n    let leftMax = 0, rightMax = 0, water = 0;\n    while (left < right) {\n        if (height[left] < height[right]) {\n            if (height[left] >= leftMax) leftMax = height[left];\n            else water += leftMax - height[left];\n            left++;\n        } else {\n            if (height[right] >= rightMax) rightMax = height[right];\n            else water += rightMax - height[right];\n            right--;\n        }\n    }\n    return water;\n}\n\n\n/**\n * 测试trap函数\n */\nfunction testTrap() {\n    console.log(\"=== trap测试 ===\");\n\n    // 测试用例\n    console.log(\"输入: height = [0,1,0,2,1,0,1,3,2,1,2,1]\");\n    console.log(\"期望输出: 6\");\n    console.log(\"说明: 上面是由数组 [0,1,0,2,1,0,1,3,2,1,2,1] 表示的高度图，在这种情况下，可以接 6 个单位的雨水（蓝色部分表示雨水）。\");\n\n    console.log(\"\\n=== 测试完成 ===\");\n}\n\n// 如果需要运行测试，取消注释下面的行\n// testTrap();",
        "python": "\"\"\"\n接雨水 - 使用双指针和两个变量来记录左右两边的最大高度\n\"\"\"\n\ndef trap(height):\n    left, right = 0, len(height) - 1\n    left_max = right_max = water = 0\n    while left < right:\n        if height[left] < height[right]:\n            if height[left] >= left_max:\n                left_max = height[left]\n            else:\n                water += left_max - height[left]\n            left += 1\n        else:\n            if height[right] >= right_max:\n                right_max = height[right]\n            else:\n                water += right_max - height[right]\n            right -= 1\n    return water\n\n\ndef test_trap():\n    \"\"\"测试trap函数\"\"\"\n    print(\"=== trap测试 ===\")\n\n    # 测试用例\n    print(\"输入: height = [0,1,0,2,1,0,1,3,2,1,2,1]\")\n    print(\"期望输出: 6\")\n    print(\"说明: 上面是由数组 [0,1,0,2,1,0,1,3,2,1,2,1] 表示的高度图，在这种情况下，可以接 6 个单位的雨水（蓝色部分表示雨水）。\")\n\n    print(\"\\n=== 测试完成 ===\")\n\n# 如果需要运行测试，取消注释下面的行\n# test_trap()"
      }
    }
  ],
  "aiExplanation": {
    "concept": {
      "title": "🧠 接雨水解题策略",
      "content": "这是一道经典的算法题目，重点学习解题思路和代码实现。"
    },
    "keyInsights": [
      "💰 理解题目的核心要求",
      "📊 选择合适的算法策略",
      "🔄 优化时间和空间复杂度"
    ],
    "commonMistakes": [
      "❌ 没有考虑边界条件",
      "❌ 算法实现有误",
      "❌ 忽略了题目的特殊要求"
    ]
  },
  "visualization": {
    "type": "step_by_step",
    "defaultData": {
      "input": "height = [0,1,0,2,1,0,1,3,2,1,2,1]",
      "output": "6",
      "explanation": "上面是由数组 [0,1,0,2,1,0,1,3,2,1,2,1] 表示的高度图，在这种情况下，可以接 6 个单位的雨水（蓝色部分表示雨水）。"
    },
    "steps": [
      {
        "step": 1,
        "description": "分析输入数据",
        "highlight": [],
        "action": "开始算法执行"
      }
    ]
  },
  "relatedProblems": [],
  "practiceExercises": [
    {
      "title": "接雨水变种练习",
      "description": "基于接雨水的变种问题",
      "difficulty": "困难",
      "hints": [
        "考虑边界情况",
        "优化算法复杂度"
      ]
    }
  ]
}